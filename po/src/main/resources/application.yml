server:
  port: 8081

spring:
  application:
    name: po

  data:
    mongodb:
      host: mongo

eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/